# AppSync AWS Lambda minimal code

this repo is to demonstrate how to call an AWS lambda from AWS AppSync, not over HTTP, but direct invocation

The lambda is dumb, because this is not how to write AWS Lambda

## Pre-requisites

- aws CLI
- terraform
- python

